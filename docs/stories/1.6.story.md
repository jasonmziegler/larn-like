# Story 1.6: Monster Reagent Consumption & Stat Bonuses

<!-- Context: Brownfield enhancement - local-only reagent consumption system, no backend persistence -->

## Status
Done

## Story
**As a** player,
**I want** to consume monster reagents to gain stat bonuses during my dungeon run,
**so that** I can experience character progression through combat and feel rewarded for killing monsters.

## Acceptance Criteria
1. Player can view collected reagents via an inventory hotkey (I or Tab)
2. Player can consume a reagent from inventory to apply its stat bonus
3. Reagent consumption grants +0.1 stat bonus to the corresponding stat (Dexterity, Strength, Constitution)
4. Stat bonuses display in the stats panel with updated values showing decimal precision
5. Multiple reagent consumptions properly stack bonuses (+0.1, +0.2, +0.3, etc.)
6. Reagent effects persist for the hero's entire lifespan and reset on death
7. Inventory panel shows reagent names, quantities, and effect descriptions

## Tasks / Subtasks

- [x] Task 1: Create Inventory Display System (AC: 1, 7)
  - [x] Create InventoryPanel.ts in apps/web/src/ui/
  - [x] Implement inventory overlay triggered by I or Tab key
  - [x] Display list of reagents in hero.inventory with names and stat descriptions
  - [x] Show quantity count for duplicate reagent types
  - [x] Add "[number] to use, [ESC] to close" instructions
  - [x] Integrate with main.ts input handling (pause game movement while inventory open)

- [x] Task 2: Implement Reagent Consumption Mechanic (AC: 2, 3)
  - [x] Add consumeReagent() function to new Inventory.ts module
  - [x] Number keys (1-9) consume corresponding inventory slot item
  - [x] Apply stat bonus to hero.currentStats based on reagent type
  - [x] Remove consumed reagent from hero.inventory
  - [x] Display consumption message: "Consumed Goblin Ear: +0.1 Dexterity"

- [x] Task 3: Implement Stat Bonus Stacking (AC: 3, 5)
  - [x] Ensure repeated consumption of same reagent type stacks correctly
  - [x] hero.currentStats uses JavaScript number type (naturally supports decimals)
  - [x] No artificial cap on stacking (limited by available reagents)
  - [x] Track total bonus per stat for display purposes

- [x] Task 4: Update Stats Display for Decimal Values (AC: 4)
  - [x] Update GameHUD.ts renderStatsPanel to show decimal stat values
  - [x] Format stats with 1 decimal place when bonuses applied (e.g., "STR: 10.1")
  - [x] Show base value vs current value when stats are modified
  - [x] Use color highlighting for stats that have been boosted (TEXT_BRIGHT vs TEXT_NORMAL)

- [x] Task 5: Implement Death Reset (AC: 6)
  - [x] Verify hero death resets currentStats to baseStats (already in createHero)
  - [x] Verify inventory is cleared on new hero creation
  - [x] Test that starting a new game after death has clean stats and empty inventory
  - [x] Add test for stat reset on death scenario

- [x] Task 6: Create Unit Tests (AC: 1-7)
  - [x] Create Inventory.test.ts in apps/web/tests/unit/game/
  - [x] Test reagent consumption applies +0.1 to correct stat
  - [x] Test stacking: 3 Goblin Ears = +0.3 Dexterity
  - [x] Test mixed reagents: different types boost different stats
  - [x] Test consumed reagent removed from inventory
  - [x] Test inventory display shows correct reagent list
  - [x] Test stats display shows decimal values after consumption
  - [x] Test death resets stats to base values
  - [x] Test inventory panel toggle (open/close)

## Dev Notes

### Existing Implementation (Brownfield Context)
**Hero.ts already provides:**
- `hero.currentStats` - mutable stats object (hp, maxHp, strength, dexterity, constitution)
- `hero.baseStats` - immutable base stats
- `hero.inventory` - array of EquipmentItem (can hold reagents)
- `createHero()` - creates fresh hero with base stats (effectively resets on new game)

**GameHUD.ts already provides:**
- `renderStatsPanel()` - displays STR, DEX, CON values
- `renderStatusBar()` - compact HP/ATK/DEF display
- Integration with CanvasRenderer

**From Story 1.5 (prerequisite):**
- Reagent items are added to `hero.inventory` on monster kill (bump-to-attack, no combat mode)
- ReagentItem type with `statBonus: { stat, amount }` field
- Combat is bump-driven; no `inCombat`/`combatTarget` state exists

**What exists and must NOT be broken:**
- Hero stat calculations (getEffectiveAttack, getEffectiveDefense)
- Combat system using hero stats
- GameHUD rendering
- Game flow (title → naming → game → death → title)

### Reagent Types (from Story 1.5)
| Reagent | Source Monster | Stat | Bonus |
|---------|---------------|------|-------|
| Goblin Ear | Goblin | Dexterity | +0.1 |
| Orc Tooth | Orc | Strength | +0.1 |
| Troll Hide | Troll | Constitution | +0.1 |
| Dragon Scale | Dragon | All stats | +0.5 |

### Inventory UI Design
```
+---------- INVENTORY ----------+
| 1. Goblin Ear (x2)    +DEX   |
| 2. Orc Tooth (x1)     +STR   |
|                               |
| [1-9] Use  [ESC] Close       |
+-------------------------------+
```
- Overlay renders on top of game view
- Game input (movement) paused while inventory open
- Simple numbered list matching keyboard shortcuts

### Stat Display Format
```
STR: 10.2 (+0.2)    <- green highlighting on boosted stats
DEX: 10.1 (+0.1)
CON: 10.0            <- no bonus indicator when at base
```

### Testing Patterns
- **Environment**: `// @vitest-environment jsdom` at file top
- **Vitest**: Primary framework
- Test reagent consumption by creating hero, adding reagent to inventory, consuming, checking stats
- Test stacking by consuming multiple reagents of same type

### Performance Requirements
- Instant stat update on reagent consumption
- Inventory panel renders at 60fps
- No delay when opening/closing inventory

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-30 | 1.0 | Initial story creation | Bob (Scrum Master) |
| 2026-01-29 | 2.0 | Revised for local-only scope - removed database persistence, API integration, DECIMAL column references | Sarah (Product Owner) |
| 2026-01-30 | 3.0 | Implementation complete - all 6 tasks done, 179 tests passing (21 new), Vite build clean. Set to Ready for Review. | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References
No debug issues encountered. All 179 tests passed on first run.

### Completion Notes List
- Created `InventoryPanel.ts` - overlay panel triggered by I/Tab, displays reagents grouped by type with quantity counts and stat labels (+DEX, +STR, +CON, +ALL)
- Created `Inventory.ts` - `consumeReagent()` function handles stat bonus application, inventory removal, and message generation
- Consumption supports `'all'` stat type (Dragon Scale applies to STR/DEX/CON simultaneously)
- Updated `GameHUD.ts` `renderStatsPanel()` with `renderStatLine()` helper - shows decimal precision (e.g., "STR: 10.1 (+0.1)") with green highlighting for boosted stats, integer display for base values
- Updated `main.ts` input handler - inventory open pauses game movement; I/Tab toggles; ESC closes; number keys 1-9 consume grouped reagent slots
- Death reset verified: `createHero()` creates fresh stats and empty inventory, no carryover between heroes
- Stacking has no artificial cap - tested up to 10x consumption (+1.0 total)
- 21 new tests in `Inventory.test.ts` covering consumption, stacking, mixed reagents, death reset, inventory panel toggle, reagent grouping, and display

### File List
| File | Action |
|------|--------|
| `apps/web/src/ui/InventoryPanel.ts` | New - inventory overlay panel with reagent display |
| `apps/web/src/game/Inventory.ts` | New - consumeReagent() function for stat bonus application |
| `apps/web/src/ui/GameHUD.ts` | Modified - renderStatsPanel uses decimal display with bonus indicators |
| `apps/web/src/main.ts` | Modified - integrated inventory toggle, consumption keybinds, inventory panel rendering |
| `apps/web/tests/unit/game/Inventory.test.ts` | New - 21 tests for reagent consumption, stacking, inventory panel |

## QA Results
*Results from QA Agent review of the completed story implementation*
