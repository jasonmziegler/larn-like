# Story 2.1: Local World State Persistence Infrastructure

## Status
Done
## Story
**As a** developer,
**I want** a persistent world state system using browser-local storage that survives hero deaths and browser sessions,
**so that** monster evolutions, teeth locations, and world changes remain permanent across gameplay sessions on the same device.

## Acceptance Criteria
1. World state stored in IndexedDB including monster locations, stats, and equipment
2. World state persists across browser refresh and tab close/reopen on the same device
3. World state data model supports monster evolution tracking, teeth location storage, and equipment histories
4. World state loads within 500ms on game startup with appropriate loading indicators
5. Storage schema is versioned to support future migration to cloud persistence (Epic 5)
6. Graceful handling of storage quota limits with user notification if world state exceeds browser storage capacity

## Tasks / Subtasks

- [x] Task 1: Update Architecture Documentation (AC: 5)
  - [x] Rewrite data-models.md "Local Persistence Architecture" section: replace localStorage key schema with IndexedDB object store design
  - [x] Define object stores: `heroes`, `monsters`, `dungeonLevels`, `deathEvents`, `soulShrines`, `teethDrops`, `worldMeta`
  - [x] Document indexes for each store (e.g., monsters by levelDepth, deathEvents by timestamp)
  - [x] Update serialization format notes (IndexedDB stores objects natively, no JSON.stringify needed)
  - [x] Keep migration path section, update to reference IndexedDB→Supabase mapping

- [x] Task 2: Create WorldStore Persistence Layer (AC: 1, 2, 5)
  - [x] Create `apps/web/src/world/WorldStore.ts`
  - [x] Implement IndexedDB database initialization with versioned schema (`larn-like-db`, version 1)
  - [x] Create object stores with appropriate key paths and indexes
  - [x] Implement CRUD methods: `saveHero()`, `loadHero()`, `saveLevel()`, `loadLevel()`, `saveDeathEvent()`, `loadDeathEvents()`, `saveWorldMeta()`, `loadWorldMeta()`
  - [x] Implement `getAll()` and `getByIndex()` helpers for queried reads
  - [x] Add schema version check on open with `onupgradeneeded` handler for future migrations

- [x] Task 3: Create WorldState Manager (AC: 1, 3)
  - [x] Create `apps/web/src/world/WorldState.ts`
  - [x] Implement `LocalWorldState` interface matching architecture data model (version, credits, currentHero, deathEvents, levels, lastSaved)
  - [x] Wire WorldState to WorldStore for load/save operations
  - [x] Implement `initializeWorld()` — load existing state or create fresh world
  - [x] Implement `saveWorld()` — persist current state to IndexedDB
  - [x] Support monster evolution tracking, teeth locations, equipment histories in the data model

- [x] Task 4: Integrate Persistence with Game Loop (AC: 2, 4)
  - [x] Modify `main.ts` to call `WorldState.initializeWorld()` on startup before rendering
  - [x] Save world state on key game events (hero death, level transition, teeth pickup)
  - [x] Ensure credit counter persists across sessions (currently resets)
  - [x] Verify state survives browser refresh and tab close/reopen

- [x] Task 5: Add Loading Indicator (AC: 4)
  - [x] Display loading state while IndexedDB initializes and world state loads
  - [x] Ensure load completes within 500ms target (measure and log if exceeded)
  - [x] Transition to title screen / game screen after load completes

- [x] Task 6: Implement Quota Management (AC: 6)
  - [x] Check `navigator.storage.estimate()` on save operations
  - [x] Display warning message when storage exceeds 80% of quota
  - [x] Catch write errors gracefully — notify player, don't crash
  - [x] Add "export save" function (download world state as JSON file)

- [x] Task 7: Create Unit Tests (AC: 1-6)
  - [x] Create `apps/web/tests/unit/world/WorldStore.test.ts`
  - [x] Test database creation and schema versioning
  - [x] Test save/load round-trip for hero, level, death event, world meta
  - [x] Test persistence across simulated "sessions" (close and reopen DB)
  - [x] Test quota error handling
  - [x] Test loading performance stays under 500ms with sample data
  - [x] Use `fake-indexeddb` or similar polyfill for test environment

## Dev Notes

### Existing Codebase Context

**Current game state lives entirely in `main.ts`** as local variables within `initGame()`. There is no persistence layer — refreshing the browser resets everything. The game currently tracks:
- Hero state (stats, equipment, inventory, position, teethCurrency)
- Dungeon level (tiles, monsters, items/teeth)
- Messages array
- Camera position
- Credits (hardcoded to 3 on startup)

**Current file structure (relevant):**
```
apps/web/src/
├── game/
│   ├── Combat.ts          # Combat processing
│   ├── DungeonGenerator.ts # Level generation
│   ├── Equipment.ts       # Equipment management
│   ├── Hero.ts            # Hero creation/stats
│   └── Inventory.ts       # Inventory management
├── ui/                    # UI screens
├── main.ts                # Game loop, state, rendering
└── (no world/ directory yet — create it)
```

**Shared types already defined in `packages/shared/src/types/`:**
- `hero.types.ts` — `Hero`, `HeroStats`, `EquipmentSlots`, `EquipmentItem`, `Position`
- `monster.types.ts` — `Monster`, `MonsterPosition`, `MonsterDefinition`
- `world.types.ts` — `WorldState`, `DeathSite`, `DeathPosition`, `Level`, `Tile`, `TileType`
- `item.types.ts` — item type definitions

**Note:** The shared `world.types.ts` has a basic `WorldState` interface that will need extending. The `DeathSite` interface already exists and is close to what's needed. The `Monster` interface in `monster.types.ts` has `evolutionPoints` field already.

### IndexedDB Design

**Database name:** `larn-like-db`
**Version:** 1

**Object stores:**

| Store | keyPath | Indexes | Purpose |
|-------|---------|---------|---------|
| `worldMeta` | `key` | — | Version, credits, lastSaved |
| `heroes` | `id` | `isAlive` | Current and past heroes |
| `dungeonLevels` | `depth` | — | Generated level layouts + contents |
| `monsters` | `id` | `levelDepth`, `isEvolved` | All monster instances |
| `deathEvents` | `id` | `timestamp`, `levelDepth` | Immutable death records |
| `soulShrines` | `id` | `levelDepth`, `isActive` | Shrine placement and state |
| `teethDrops` | `id` | `levelDepth` | Teeth at death sites |

**Key architectural decisions:**
- Use raw IndexedDB API (no library dependency) — the wrapper is thin enough
- All DB operations are async — use `async/await` with Promise-wrapped transactions
- `onupgradeneeded` handler creates stores on first visit, handles migrations on version bump
- Store objects natively — no JSON.stringify needed
- Dates stored as ISO strings for IndexedDB compatibility

### Migration Path (Epic 5 reference)

The object store design mirrors future Supabase tables. When Epic 5 arrives, swap `WorldStore` implementation from IndexedDB to Supabase client calls. The `WorldState` manager interface stays the same.

### Current Placeholder Teeth

`main.ts:120-137` places random teeth on levels as placeholders (marked with TODO comment). Story 2.1 should preserve this behavior for now. Story 2.4 will replace placeholders with death-event-generated teeth.

### Testing

- **Test location:** `apps/web/tests/unit/world/`
- **Framework:** Vitest
- **IndexedDB in tests:** Use `fake-indexeddb` npm package to polyfill IndexedDB in Node test environment
- **Pattern:** Follow existing test patterns in `apps/web/tests/unit/game/Combat.test.ts`
- **Performance test:** Use `performance.now()` to measure load time, assert < 500ms

## Dev Agent Record

### Agent Model Used
Claude Opus 4.5

### File List
| File | Action | Description |
|------|--------|-------------|
| `apps/web/src/world/WorldStore.ts` | Created | IndexedDB persistence layer with CRUD operations for all object stores |
| `apps/web/src/world/WorldState.ts` | Created | World state manager with initialization, save/load, quota management, and export |
| `apps/web/src/main.ts` | Modified | Integrated WorldState persistence — loading indicator, credit sync, save on death/teeth pickup |
| `apps/web/tests/unit/world/WorldStore.test.ts` | Created | 19 unit tests covering schema, CRUD, sessions, quota, performance |
| `apps/web/package.json` | Modified | Added fake-indexeddb devDependency |
| `docs/architecture/data-models.md` | Unchanged | Already had IndexedDB schema documentation (pre-existing) |

### Debug Log References
No debug issues encountered.

### Completion Notes
- All 7 tasks completed with all subtasks checked
- 197 tests pass across 10 test files (19 new WorldStore tests, 0 regressions)
- WorldStore uses raw IndexedDB API with Promise-wrapped transactions
- WorldState manages in-memory state synced to IndexedDB on key game events
- Credits now persist via IndexedDB (TitleScreen localStorage kept as fallback, WorldState is source of truth)
- Loading indicator displayed during IndexedDB initialization with performance logging
- Quota management: checks navigator.storage.estimate(), warns at 80%, graceful failure on write errors
- Export save function downloads world state as JSON file

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-30 | v1.0 | Initial story draft | Sarah (PO Agent) |
| 2026-01-31 | v1.1 | Implementation complete — all tasks done | James (Dev Agent) |
