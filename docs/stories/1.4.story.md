# Story 1.4: ASCII Dungeon Rendering & Movement

## Status
Draft

## Story
**As a** player,
**I want** to navigate through ASCII dungeon levels using traditional roguelike controls,
**so that** I can explore the game world with familiar and responsive movement mechanics.

## Acceptance Criteria
1. Dungeon level renders as ASCII characters with walls (#), floors (.), and monsters (various letters)
2. WASD/arrow keys/numpad movement works smoothly with immediate visual response
3. Player character (@) moves one tile per keypress without sliding or animation delays
4. Movement collision detection prevents walking through walls or monsters
5. Screen viewport follows player movement, scrolling appropriately when player approaches edges
6. Dungeon layout includes rooms, corridors, and at least 3 different monster types visually distinct

## Tasks / Subtasks

- [ ] Task 1: Create Dungeon Level Rendering System (AC: 1, 6)
  - [ ] Create LevelManager.ts component in apps/web/src/world/
  - [ ] Implement dungeon level loading and data management
  - [ ] Create ASCII rendering for walls (#), floors (.), stairs
  - [ ] Implement room and corridor rendering from LevelLayout data
  - [ ] Create monster rendering with distinct ASCII characters (S, V, R, etc.)
  - [ ] Integrate with CanvasRenderer for efficient level display

- [ ] Task 2: Implement Movement Input System (AC: 2, 3)
  - [ ] Create Movement.ts component in apps/web/src/game/
  - [ ] Integrate with InputHandler.ts for WASD/arrow keys/numpad
  - [ ] Implement tile-based movement (one tile per keypress)
  - [ ] Create immediate visual response without sliding animations
  - [ ] Add input validation and movement queue management
  - [ ] Optimize movement performance for responsive gameplay

- [ ] Task 3: Create Collision Detection System (AC: 4)
  - [ ] Create Collision.ts utilities in apps/web/src/utils/
  - [ ] Implement wall collision detection (prevent moving through #)
  - [ ] Create monster collision detection (prevent moving through monsters)
  - [ ] Add boundary collision detection for level edges
  - [ ] Implement collision validation for movement attempts
  - [ ] Create collision feedback and error handling

- [ ] Task 4: Implement Viewport and Camera System (AC: 5)
  - [ ] Create viewport management in CanvasRenderer.ts
  - [ ] Implement camera following for player movement
  - [ ] Create smooth viewport scrolling when player approaches edges
  - [ ] Add viewport bounds checking and level boundary handling
  - [ ] Implement efficient rendering of visible tiles only
  - [ ] Create viewport coordinate transformation system

- [ ] Task 5: Create Monster Positioning and Display (AC: 1, 6)
  - [ ] Create Monster.ts component in apps/web/src/game/
  - [ ] Implement monster positioning and coordinate management
  - [ ] Create distinct ASCII characters for 3+ monster types
  - [ ] Add monster rendering integration with level display
  - [ ] Implement monster visibility and viewport culling
  - [ ] Create monster animation preparation (static for now)

- [ ] Task 6: Integrate Dungeon Systems with Game Engine (AC: 1-6)
  - [ ] Connect level rendering with Game Engine state management
  - [ ] Integrate hero positioning with dungeon coordinate system
  - [ ] Create level transition preparation (stairs functionality)
  - [ ] Add performance optimization for real-time rendering
  - [ ] Implement dungeon state persistence and caching
  - [ ] Create comprehensive testing for all dungeon systems

## Dev Notes

### Previous Story Insights
Story 1.3 (Hero Creation) establishes the Hero component and basic rendering infrastructure that this story will extend for dungeon exploration. The hero (@) character and positioning system foundation is already established.

### Dungeon Level Data Model and Layout
[Source: architecture/data-models.md]
**DungeonLevel Interface:**
```typescript
interface DungeonLevel {
  id: string;
  depth: number;
  layout: LevelLayout;
  monsters: Monster[];
  chests: DungeonChest[];
  teethLocations: TeethDrop[];
  shrines: SoulShrine[];
  generatedAt: Date;
  lastRegenerated: Date;
}

interface LevelLayout {
  width: number;
  height: number;
  rooms: Room[];
  corridors: Corridor[];
  stairsUp: Position;
  stairsDown: Position;
}
```

**ASCII Character Mapping:**
- **Walls**: # (hash character for solid barriers)
- **Floors**: . (period character for walkable space)
- **Player**: @ (at symbol for hero character)
- **Stairs Down**: > (greater than for level transitions)
- **Stairs Up**: < (less than for upward level transitions)

### Monster Types and ASCII Representation
[Source: architecture/data-models.md]
**Monster Interface and Types:**
```typescript
interface Monster {
  id: string;
  type: MonsterType;
  level: number;
  evolutionLevel: number;
  stats: MonsterStats;
  equipment: EquipmentSlots;
  killHistory: KillRecord[];
  isEvolved: boolean;
  queuePosition: number | null;
  lastActive: Date;
}
```

**Minimum 3 Monster Types (AC: 6):**
- **Skeleton**: S (uppercase S for skeletal enemies)
- **Vampire Bat**: V (uppercase V for flying enemies)
- **Rat**: R (uppercase R for small ground enemies)
- Additional types can use other letters (T, O, D, etc.)

### Movement and Input Architecture
[Source: architecture/frontend-architecture.md]
**Movement.ts Component (`apps/web/src/game/Movement.ts`):**
- **Responsibility**: Movement and collision handling
- **Features**: Tile-based movement, collision detection, coordinate management
- **Dependencies**: Hero.ts, Collision.ts, LevelManager.ts

**InputHandler.ts Integration:**
- **WASD Movement**: W (up), A (left), S (down), D (right)
- **Arrow Keys**: ↑ ↓ ← → (traditional roguelike movement)
- **Numpad Movement**: 8 (up), 4 (left), 2 (down), 6 (right), plus diagonals
- **Immediate Response**: No input lag, instant visual feedback

### Collision Detection System
[Source: architecture/frontend-architecture.md]
**Collision.ts Utilities (`apps/web/src/utils/Collision.ts`):**
- **Wall Collision**: Prevent movement into # characters
- **Monster Collision**: Prevent movement into occupied tiles
- **Boundary Collision**: Prevent movement outside level bounds
- **Collision Validation**: Pre-movement validation for responsive feedback

**Collision Detection Patterns:**
```typescript
// Expected collision detection API
class Collision {
  static isValidMove(
    currentPos: Position,
    targetPos: Position,
    level: DungeonLevel
  ): boolean {
    // Validate wall, monster, and boundary collisions
    return !this.isWall(targetPos, level) &&
           !this.hasMonster(targetPos, level) &&
           this.isInBounds(targetPos, level);
  }
}
```

### Rendering Engine Integration
[Source: architecture/frontend-architecture.md]
**CanvasRenderer.ts Integration:**
- **Main Rendering Coordinator**: Canvas 2D context management
- **ASCIIAtlas.ts**: Character sprite management for all ASCII characters
- **Performance Optimization**: Dirty rectangle optimization and viewport culling
- **60fps Rendering**: Efficient level rendering for smooth gameplay

**Viewport and Camera System:**
```typescript
// Expected viewport management
class CanvasRenderer {
  renderLevel(level: DungeonLevel, heroPos: Position) {
    this.updateViewport(heroPos);
    this.renderVisibleTiles(level);
    this.renderHero(heroPos);
    this.renderMonsters(level.monsters);
  }
}
```

### Level Management Architecture
[Source: architecture/frontend-architecture.md]
**LevelManager.ts Component (`apps/web/src/world/LevelManager.ts`):**
- **Responsibility**: Dungeon level handling and management
- **Features**: Level loading, caching, coordinate systems
- **Dependencies**: WorldState.ts, SyncService.ts, Database integration

**Level State Management:**
- Local level caching for responsive gameplay
- Background synchronization for persistent world state
- Level generation integration (future expansion)

### Game Engine State Integration
[Source: architecture/frontend-architecture.md]
**Game State Coordination:**
```typescript
interface GameState {
  currentHero: Hero | null;
  localLevel: DungeonLevel | null; // Current dungeon level
  inputState: InputState;
  uiState: UIState;

  // Persistent world state (synced with backend)
  worldLevels: Map<number, DungeonLevel>;
  evolvedMonsters: Map<string, Monster>;
}
```

**Local-First Movement:**
- Immediate local state updates for responsive movement
- Background synchronization for position persistence
- Optimistic updates with conflict resolution

### Performance Optimization Requirements
[Source: architecture/frontend-architecture.md]
**Rendering Performance:**
- **60fps Target**: Smooth movement and responsive controls
- **Viewport Culling**: Render only visible tiles for performance
- **Dirty Rectangle Optimization**: Update only changed screen areas
- **Memory Management**: Efficient level caching and garbage collection

### ASCII Character Utilities
[Source: architecture/frontend-architecture.md]
**ASCII.ts Utilities (`apps/web/src/utils/ASCII.ts`):**
- ASCII character utilities for consistent character rendering
- Character sprite optimization for performance
- Font styling and terminal aesthetic coordination

### Input System Architecture
**Movement Input Patterns:**
- **Traditional Roguelike**: Support all standard movement keys
- **Immediate Response**: Sub-50ms input to visual response
- **Input Validation**: Prevent invalid movement attempts
- **Input Queue**: Handle rapid keypress sequences properly

### Color and Visual Styling
[Source: architecture/frontend-architecture.md]
**ColorManager.ts Integration:**
- Green intensity system for authentic terminal aesthetic
- Character highlighting for hero and important elements
- Consistent color theming across dungeon elements

### Testing

**Test File Locations:**
[Source: architecture/testing-strategy.md]
- Frontend unit tests: `apps/web/tests/unit/game/` and `apps/web/tests/unit/world/`
- Frontend integration tests: `apps/web/tests/integration/`
- Canvas mocking: Use `mockCanvas()` for rendering tests

**Testing Frameworks and Patterns:**
- **Vitest**: Primary testing framework for movement and rendering logic
- **Canvas Mocking**: Mock Canvas rendering for level display testing
- **Component Testing**: Test Movement, LevelManager, and Collision components in isolation
- **Integration Testing**: Test complete dungeon exploration flow

**Specific Testing Requirements for this Story:**
- Test dungeon level ASCII rendering with walls, floors, and monsters
- Test movement input handling for WASD/arrow keys/numpad
- Test tile-based movement without sliding animations
- Test collision detection for walls, monsters, and boundaries
- Test viewport scrolling and camera following behavior
- Test monster positioning and distinct ASCII character display
- Test level rendering performance and 60fps capability
- Test integration between movement, rendering, and collision systems

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-30 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results
*Results from QA Agent review of the completed story implementation*