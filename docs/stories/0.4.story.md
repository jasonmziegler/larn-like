# Story 0.4: Development Environment Verification

## Status
Draft

## Story
**As a** developer,
**I want** a validated development environment setup with all services integrated,
**so that** I can immediately begin game development with confidence in the infrastructure.

## Acceptance Criteria
1. Local development server runs without errors and connects to Supabase services
2. Authentication flow tested with user registration, login, and session management
3. Database CRUD operations verified with test data insertion and retrieval
4. Real-time subscriptions tested with live data updates
5. Canvas 2D rendering context initialized and displaying basic ASCII characters
6. Build and deployment pipeline tested with successful production deployment

## Tasks / Subtasks

- [ ] Task 1: Verify Local Development Server Integration (AC: 1)
  - [ ] Test npm run dev command starts all services without errors
  - [ ] Verify frontend development server (localhost:5173) loads successfully
  - [ ] Verify backend API server (localhost:3000/api) responds correctly
  - [ ] Test Supabase connection with development environment variables
  - [ ] Verify hot reload functionality for both frontend and backend
  - [ ] Test environment variable loading and configuration validation

- [ ] Task 2: Test Authentication Flow Integration (AC: 2)
  - [ ] Create test user registration with email/password authentication
  - [ ] Verify user login and JWT token generation
  - [ ] Test session management and automatic token refresh
  - [ ] Verify secure logout and session cleanup
  - [ ] Test OAuth providers if configured (Google, GitHub)
  - [ ] Validate authentication middleware for API routes

- [ ] Task 3: Verify Database CRUD Operations (AC: 3)
  - [ ] Test player creation and profile management
  - [ ] Test hero creation with base stats and equipment
  - [ ] Test equipment item creation and modification
  - [ ] Test monster creation and evolution data updates
  - [ ] Test dungeon level creation and layout storage
  - [ ] Verify Row Level Security (RLS) policies block unauthorized access

- [ ] Task 4: Test Real-time Subscription Integration (AC: 4)
  - [ ] Test real-time subscriptions for monster table updates
  - [ ] Test real-time subscriptions for soul_shrines table changes
  - [ ] Test real-time subscriptions for death_events processing
  - [ ] Verify subscription cleanup and connection management
  - [ ] Test real-time data synchronization across multiple clients
  - [ ] Validate real-time performance with multiple concurrent connections

- [ ] Task 5: Verify Canvas 2D Rendering System (AC: 5)
  - [ ] Initialize HTML5 Canvas 2D context successfully
  - [ ] Test basic ASCII character rendering and display
  - [ ] Verify ColorManager for green intensity terminal aesthetic
  - [ ] Test CanvasRenderer coordinate system and viewport
  - [ ] Verify 60fps rendering loop and performance monitoring
  - [ ] Test responsive canvas resizing and layout management

- [ ] Task 6: Test Build and Deployment Pipeline (AC: 6)
  - [ ] Test local production build with npm run build
  - [ ] Verify TypeScript compilation across all packages
  - [ ] Test Vercel deployment configuration and environment variables
  - [ ] Verify production Supabase connection and database access
  - [ ] Test CI/CD pipeline with automated testing and deployment
  - [ ] Validate production environment security and performance

- [ ] Task 7: Create Comprehensive Verification Script
  - [ ] Create scripts/verify-setup.js for automated environment validation
  - [ ] Add database connectivity testing and schema validation
  - [ ] Add authentication flow testing with test user creation
  - [ ] Add real-time subscription testing and cleanup
  - [ ] Add Canvas rendering verification and performance testing
  - [ ] Add deployment verification and production connectivity testing

## Dev Notes

### Previous Story Insights
This story validates the complete infrastructure setup from Stories 0.1 (Supabase Account Setup), 0.2 (Environment Configuration), and 0.3 (Database Schema Deployment). Story 1.1 (Project Foundation) also provides the monorepo structure being verified.

### Development Environment Integration
[Source: architecture/development-workflow.md]
**Development Commands:**
```bash
# Start all services
npm run dev

# Start frontend only (http://localhost:5173)
npm run dev:web

# Start backend only (http://localhost:3000/api)
npm run dev:api

# Run tests
npm run test
npm run test:e2e
npm run test:coverage
```

**Environment Variable Validation:**
```bash
# Required Frontend Variables
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
VITE_API_BASE_URL=http://localhost:3000/api

# Required Backend Variables
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
DATABASE_URL=your-database-connection-string
```

### System Integration Verification Script
[Source: architecture/infrastructure-setup-configuration.md]
**Verification Script Pattern:**
```typescript
// Create test script: scripts/verify-setup.js
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  process.env.VITE_SUPABASE_URL!,
  process.env.VITE_SUPABASE_ANON_KEY!
);

async function verifySetup() {
  // Test database connection
  const { data, error } = await supabase.from('players').select('count');
  console.log('Database connection:', error ? 'FAILED' : 'SUCCESS');

  // Test authentication
  const { data: authData, error: authError } = await supabase.auth.signUp({
    email: 'test@example.com',
    password: 'testpassword123'
  });
  console.log('Authentication:', authError ? 'FAILED' : 'SUCCESS');

  // Test real-time subscriptions
  const subscription = supabase
    .channel('test')
    .on('postgres_changes', { event: '*', schema: 'public', table: 'players' },
        (payload) => console.log('Real-time:', 'SUCCESS'))
    .subscribe();

  console.log('Setup verification complete');
}
```

### Authentication Integration Testing
[Source: architecture/backend-architecture.md]
**Authentication Patterns:**
- JWT token validation for API routes
- Automatic token refresh and session management
- Secure logout and session cleanup
- OAuth provider integration testing

**Middleware Testing:**
```typescript
// Authentication middleware testing
export async function authenticate(request: NextRequest): Promise<User> {
  const authHeader = request.headers.get('authorization');
  // ... validation logic
  return user;
}
```

### Database Integration Verification
[Source: architecture/database-schema.md]
**CRUD Operation Testing:**
- Test all table operations (players, heroes, equipment_items, monsters, etc.)
- Verify foreign key constraints and relationships
- Test RLS policies with different user scenarios
- Validate data integrity and constraints

**Performance Validation:**
```sql
-- Test performance optimization indexes
CREATE INDEX CONCURRENTLY idx_monsters_level_active ON monsters(current_level) WHERE queue_position IS NULL;
CREATE INDEX CONCURRENTLY idx_equipment_search ON equipment_items(type, rarity, is_blessed);
```

### Real-time Subscription Testing
[Source: architecture/infrastructure-setup-configuration.md]
**Real-time Tables to Test:**
```sql
-- Enable real-time for required tables
ALTER publication supabase_realtime ADD TABLE monsters;
ALTER publication supabase_realtime ADD TABLE soul_shrines;
ALTER publication supabase_realtime ADD TABLE death_events;
```

**Subscription Testing Patterns:**
- Test connection establishment and cleanup
- Validate data synchronization across multiple clients
- Test subscription performance with concurrent connections
- Verify reconnection handling and error recovery

### Canvas 2D Rendering Verification
[Source: architecture/frontend-architecture.md]
**Canvas System Components:**
- **CanvasRenderer.ts**: Main rendering coordinator
- **ASCIIAtlas.ts**: Character sprite management
- **ColorManager.ts**: Green intensity system for terminal aesthetic
- **LayoutManager.ts**: Screen layout (map, UI panels)

**Performance Requirements:**
- 60fps rendering loop with performance monitoring
- Responsive canvas resizing and viewport management
- Optimized ASCII character rendering

### Build and Deployment Verification
[Source: architecture/deployment-architecture.md]
**Deployment Environments:**
- Development: http://localhost:5173 (frontend), http://localhost:3000/api (backend)
- Production: https://larn-like.vercel.app

**CI/CD Pipeline Testing:**
```yaml
# Verify pipeline components
- run: npm run lint
- run: npm run type-check
- run: npm run test
- run: npm run test:e2e
- run: npm run build
```

### Monitoring and Performance Validation
[Source: architecture/monitoring-and-observability.md]
**Key Metrics to Verify:**
- **Frontend**: Core Web Vitals, JavaScript errors, API response times
- **Backend**: Request rate, response time, error rate, database query performance
- **Game-Specific**: Canvas rendering performance, session duration, event processing time

### High-Level Architecture Integration
[Source: architecture/high-level-architecture.md]
**System Integration Flow:**
```mermaid
Browser Client --> Game Canvas UI --> Local Game Engine --> World State Sync --> Vercel Edge Functions --> Supabase PostgreSQL
```

**Local-First Architecture Validation:**
- Responsive local gameplay (sub-200ms input response)
- Background synchronization for world state changes
- Client-side game mechanics with server-side world persistence

### Security Validation
[Source: architecture/security-and-performance.md]
**Security Requirements to Test:**
- **Frontend Security**: CSP Headers, XSS Prevention, Secure Storage
- **Backend Security**: Input Validation, Rate Limiting, CORS Policy
- **Authentication Security**: JWT tokens, Session Management, Password Policy

### Testing

**Test File Locations:**
[Source: architecture/testing-strategy.md]
- Integration tests: `apps/web/tests/integration/` and `apps/api/tests/integration/`
- E2E tests: `tests/e2e/`
- Environment verification: `scripts/verify-setup.js`

**Testing Frameworks and Patterns:**
- **Vitest**: Primary testing framework for all verification testing
- **Playwright**: E2E testing for full system integration
- **Custom Verification**: Environment setup validation scripts

**Specific Testing Requirements for this Story:**
- Test complete development environment setup and service integration
- Test authentication flow with user registration, login, and session management
- Test database CRUD operations with all tables and RLS policies
- Test real-time subscriptions with live data updates and synchronization
- Test Canvas 2D rendering with ASCII characters and performance validation
- Test build and deployment pipeline with production environment verification
- Create comprehensive verification script for automated environment validation

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-09-30 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results
*Results from QA Agent review of the completed story implementation*