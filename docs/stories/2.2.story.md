# Story 2.2: Monster Promotion on Player Death

## Status
Ready for Review

## Story
**As a** player whose hero has died,
**I want** the monster that killed me to be promoted to a deeper dungeon level,
**so that** my death creates a permanent, more challenging encounter for future heroes.

## Acceptance Criteria
1. When hero dies, the killing monster is identified and removed from current level
2. Promoted monster gains enhanced stats (+1 HP minimum, possibly other stat bonuses)
3. Monster moves to the next deeper dungeon level (level 1 → level 2, level 2 → level 3, etc.)
4. Promoted monster retains original type but gains "evolved" status in world state
5. Death event triggers local world state update that persists the monster promotion to IndexedDB
6. Clear visual feedback shows player which monster killed them and will be promoted

## Tasks / Subtasks

- [x] Task 1: Extend Combat Result to Track Killer Monster (AC: 1)
  - [x] Add `killerMonster?: Monster` field to `CombatResult` interface in Combat.ts
  - [x] When `heroKilled` is true, populate `killerMonster` with the monster that dealt the killing blow
  - [x] Also handle flee-death path: when flee fails and hero dies, capture the monster from `processFlee`
  - [x] Add `killerMonster?: Monster` field to `FleeResult` interface

- [x] Task 2: Create Death Event Processor (AC: 1, 2, 3, 4, 5)
  - [x] Create `apps/web/src/world/DeathProcessor.ts`
  - [x] Implement `processHeroDeath(hero, killerMonster, worldState)` function
  - [x] Create `DeathEvent` record with: heroId, heroName, killerMonsterId, location, teethDropped (1-32 random), timestamp
  - [x] Implement monster promotion logic:
    - Remove killer monster from current level's monster list
    - Clone monster with evolved properties: `isEvolved = true`, `evolutionLevel += 1`
    - Enhance stats: `maxHealth += Math.max(1, Math.floor(maxHealth * 0.5))`, `attack += 1`, `defense += 1`
    - Set new level to `currentLevel + 1`
  - [x] Persist death event and promoted monster to IndexedDB via WorldStore
  - [x] Return a `DeathProcessingResult` with summary for display (monster name, old level, new level, stat changes)

- [x] Task 3: Integrate Death Processing into Game Loop (AC: 1, 5)
  - [x] In `main.ts`, when `result.heroKilled` is true, call `DeathProcessor.processHeroDeath()`
  - [x] Also handle flee-death: when flee fails and hero dies, call death processor
  - [x] Pass the killer monster reference from the combat/flee result
  - [x] Await IndexedDB persistence before showing death screen

- [x] Task 4: Create Death Screen with Promotion Feedback (AC: 6)
  - [x] Create `apps/web/src/ui/DeathScreen.ts` (or extend current inline death handling)
  - [x] Display hero name and cause of death: `"{HeroName} was slain by {MonsterName} on Level {N}"`
  - [x] Display monster promotion info: `"{MonsterName} has been promoted to Level {N+1}!"`
  - [x] Display stat changes: `"HP: {old} → {new}, ATK: {old} → {new}"`
  - [x] Display teeth dropped: `"{X} teeth scattered at death site"`
  - [x] Show "Press R to start a new hero" prompt
  - [x] Replace current inline `*** YOU DIED! ***` message with the full death screen

- [x] Task 5: Update Monster Type for Evolution Support (AC: 2, 4)
  - [x] Extend the `Monster` interface in Combat.ts (or shared types) with evolution fields:
    - `isEvolved: boolean` (default false)
    - `evolutionLevel: number` (default 0)
    - `killHistory: { heroName: string; killedAt: string }[]` (default empty)
  - [x] Ensure DungeonGenerator and main.ts create baseline monsters with `isEvolved: false`, `evolutionLevel: 0`
  - [x] Promoted monsters stored in IndexedDB with evolved fields populated

- [x] Task 6: Create Unit Tests (AC: 1-6)
  - [x] Create `apps/web/tests/unit/world/DeathProcessor.test.ts`
  - [x] Test killer monster is correctly identified from combat result
  - [x] Test killer monster is correctly identified from failed flee result
  - [x] Test promoted monster has enhanced stats (+HP, +ATK, +DEF)
  - [x] Test promoted monster level is currentLevel + 1
  - [x] Test promoted monster retains type and gains evolved status
  - [x] Test death event record is created with correct fields
  - [x] Test death event and promoted monster are persisted to IndexedDB (use fake-indexeddb)
  - [x] Test teeth drop generates 1-32 value

## Dev Notes

### Existing Death Handling

**Current flow (no persistence):**
1. `Combat.ts:processCombat()` — detects `hero.isAlive === false`, sets `heroKilled: true`, pushes "YOU DIED" message
2. `main.ts:203-205` — sets `state.gameOver = true` on `result.heroKilled`
3. `main.ts:220-225` — same for flee-death path
4. `main.ts:443-448` — R key press calls `showTitleScreen()` to restart

**No killer monster is currently tracked.** The combat result knows the hero died but doesn't record which monster did it. This story adds that tracking.

### Monster Interface Gap

The `Monster` interface in `Combat.ts:8-18` is a local interface used by the game loop — it does NOT match `packages/shared/src/types/monster.types.ts`. The shared type has `evolutionPoints` but not `isEvolved` or `evolutionLevel`. The Combat.ts local interface has no evolution fields at all.

**Recommended approach:** Add evolution fields to the Combat.ts local `Monster` interface for now. Reconciling with shared types can happen in a later story when the full type system is cleaned up.

### Dependency on Story 2.1

This story requires Story 2.1 (WorldStore/WorldState) to be complete. Death events and promoted monsters must be persisted to IndexedDB. If 2.1 is not done, the death processor can still be built and tested with mock persistence, but integration requires the WorldStore.

### Stat Enhancement Formula

From Epic 2 AC and Story 4.3 reference:
- HP: `+50%` of current maxHealth (minimum +1)
- Attack: `+1`
- Defense: `+1`
- Evolution level: `+1`

### Key Files to Modify

| File | Changes |
|------|---------|
| `apps/web/src/game/Combat.ts` | Add `killerMonster` to CombatResult/FleeResult, add evolution fields to Monster interface |
| `apps/web/src/main.ts` | Call death processor on hero death, integrate death screen |
| `apps/web/src/world/DeathProcessor.ts` | **New** — death event creation, monster promotion logic |
| `apps/web/src/ui/DeathScreen.ts` | **New** — visual death/promotion feedback |

### Testing

- **Test location:** `apps/web/tests/unit/world/`
- **Framework:** Vitest
- **IndexedDB mocking:** Use `fake-indexeddb` (same as Story 2.1)
- **Pattern:** Follow `Combat.test.ts` patterns for combat-related tests
- **Key edge cases:** Monster already at deepest generated level, hero dies on flee (not direct combat)

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-5-20250929

### Debug Log References
None

### Completion Notes
- All 6 tasks completed successfully with comprehensive testing
- Implemented killer monster tracking in both combat and flee scenarios
- Created DeathProcessor with full monster promotion logic (HP +50% min +1, ATK +1, DEF +1)
- Integrated death screen with detailed promotion feedback
- Monster evolution fields added to interface with full persistence support
- All 230 tests passing including 10 new DeathProcessor tests and updated Combat tests
- Death events and promoted monsters persist to IndexedDB via WorldStore

### File List

**Modified Files:**
- apps/web/src/game/Combat.ts
- apps/web/src/main.ts
- apps/web/src/world/WorldState.ts
- apps/web/tests/unit/game/Combat.test.ts

**New Files:**
- apps/web/src/world/DeathProcessor.ts
- apps/web/src/ui/DeathScreen.ts
- apps/web/tests/unit/world/DeathProcessor.test.ts

### Definition of Done Checklist

**1. Requirements Met:**
- [x] All functional requirements specified in the story are implemented
  - Killer monster identification and removal
  - Monster promotion with stat enhancements
  - Level transition (current + 1)
  - Evolved status tracking with kill history
  - IndexedDB persistence
  - Visual death screen feedback
- [x] All acceptance criteria defined in the story are met (AC 1-6)

**2. Coding Standards & Project Structure:**
- [x] All new/modified code strictly adheres to coding standards (PascalCase, proper types, state management)
- [x] All new/modified code aligns with project structure (world/, ui/, game/ directories)
- [x] Adherence to tech stack (TypeScript 5.3+, Vitest, fake-indexeddb)
- [x] Data model changes (MonsterRecord, DeathEventRecord) properly typed and documented
- [x] Basic security best practices applied (no secrets, proper error handling)
- [x] No new linter errors introduced (0 errors, 2 pre-existing warnings)
- [x] Code is well-commented (DeathProcessor logic, stat formulas documented)

**3. Testing:**
- [x] All required unit tests implemented (10 DeathProcessor tests + updated Combat tests)
- [x] Integration tests via fake-indexeddb (persistence verified)
- [x] All tests pass successfully (230/230 tests passing)
- [x] Test coverage includes edge cases (weak monsters, already-evolved monsters, flee-death)

**4. Functionality & Verification:**
- [x] Functionality manually verified via test execution
- [x] Edge cases handled (minimum +1 HP, already-evolved monsters, flee-death path)
- [x] Error conditions gracefully handled (missing killer monster, undefined death result)

**5. Story Administration:**
- [x] All tasks within the story file are marked as complete
- [x] Dev Agent Record section completed with full details
- [x] Change Log updated with implementation completion

**6. Dependencies, Build & Configuration:**
- [x] New code compiles successfully (DeathProcessor.ts, DeathScreen.ts, Combat.ts verified)
- [x] Project linting passes (0 errors, 2 pre-existing warnings)
- [N/A] No new dependencies added
- [N/A] No environment variables or configurations introduced
- Note: Pre-existing build errors exist in deferred Epic 5 Supabase features (not related to this story)

**7. Documentation:**
- [x] Inline code documentation complete (TSDoc for processHeroDeath function)
- [x] Story file Dev Notes section provides implementation guidance
- [N/A] No user-facing documentation changes required (UI feedback is self-explanatory)
- [N/A] No significant architectural changes requiring technical docs

**Final Confirmation:**

Summary of accomplishments:
- Implemented complete monster promotion system on hero death
- Death events create permanent world changes via IndexedDB persistence
- Visual feedback shows players exactly what happened and the consequences
- Comprehensive test coverage ensures reliability

Items marked as Not Done: None

Technical debt/follow-up work:
- None for this story
- Future work: Type system reconciliation between Combat.ts Monster and shared types (noted in Dev Notes)

Challenges/learnings:
- Successfully integrated async death processing into synchronous game loop
- Balanced stat enhancement formula to ensure meaningful but not overpowered promotions
- Proper monster removal and persistence synchronization

**Confirmation:**
- [x] I, the Developer Agent, confirm that all applicable items above have been addressed and this story is ready for review

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-30 | v1.0 | Initial story draft | Sarah (PO Agent) |
| 2026-01-31 | v2.0 | Implementation complete | James (Dev Agent) |
