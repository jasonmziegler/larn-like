# Story 2.7: Soul Shrine Creation on Hero Death

## Status
Done

## Story
**As a** player whose hero has died,
**I want** a soul shrine to appear at the death location,
**so that** my hero's sacrifice leaves a permanent, interactive monument in the world.

## Acceptance Criteria
1. When a hero dies, a soul shrine is created at the exact death coordinates alongside teeth currency
2. Shrine displays as a distinct ASCII symbol (e.g., `†`) on the dungeon map
3. Shrine stores metadata: deceased hero name, level at death, and soul energy value
4. Soul energy value scales with hero level and stats at time of death
5. Shrine persists in browser-local world state until consumed by a future hero's blessing attempt
6. Multiple shrines can exist on the same dungeon level from different hero deaths

## Tasks / Subtasks

- [x] Task 1: Define SoulShrine Entity Type (AC: 3, 4) ✅ COMPLETE
  - [x] Create `apps/web/src/world/SoulShrine.ts`
  - [x] Define `SoulShrine` interface:
    ```typescript
    interface SoulShrine {
      id: string;
      heroName: string;
      heroLevel: number;
      levelDepth: number;
      position: { x: number; y: number };
      soulEnergy: number;
      isActive: boolean;
      createdAt: string; // ISO 8601
    }
    ```
  - [x] Implement `calculateSoulEnergy(hero)` function:
    - Base energy = `hero.level * 10`
    - Stat bonus = `Math.floor(hero.currentStats.strength + hero.currentStats.dexterity + hero.currentStats.constitution)`
    - Total = base + stat bonus (minimum 10, so even a level 1 hero produces a usable shrine)
  - [x] Implement `createSoulShrine(hero, deathPosition)` factory function that generates a SoulShrine from a dying hero

- [x] Task 2: Add SoulShrine Persistence to WorldStore (AC: 5, 6) ✅ COMPLETE
  - [x] Add `saveSoulShrine(shrine)` to WorldStore.ts — saves to `soulShrines` object store
  - [x] Add `loadShrinesByLevel(depth)` — returns all active shrines on a given dungeon level
  - [x] Add `consumeShrine(id)` — sets `isActive = false` (consumed by blessing, Story 2.8)
  - [x] Active shrines are indexed by `levelDepth` and `isActive` for efficient queries
  - [x] Consumed shrines remain in the store for history but are excluded from level load queries

- [x] Task 3: Create Shrine on Hero Death (AC: 1, 3, 4) ✅ COMPLETE
  - [x] In `DeathProcessor.ts:processHeroDeath()`, after creating the DeathEvent and TeethDrop, create a SoulShrine
  - [x] Shrine position matches the hero's exact death coordinates
  - [x] Shrine level matches the current dungeon depth
  - [x] Persist the SoulShrine to IndexedDB via WorldStore
  - [x] Add `soulShrineCreated: boolean` and `soulShrineId: string` to the `DeathProcessingResult`
  - [x] Update the `DeathEvent` record's `soulShrineCreated` field to `true`

- [x] Task 4: Load and Render Shrines on Dungeon Map (AC: 2, 5, 6) ✅ COMPLETE
  - [x] On level initialization in `main.ts`, query WorldStore for active shrines on the current level
  - [x] Add shrines to a new `shrines` array in GameState (or merge into a generic entities array)
  - [x] Render shrines as `†` character in a distinct color (purple/magenta: `#CC66FF`) at their map positions
  - [x] Shrines do not block movement — hero can walk onto a shrine tile
  - [x] Multiple shrines on the same level each render at their own position
  - [x] Since dungeon layouts regenerate per hero (Story 2.6 design), place shrines at valid floor positions on each new layout

- [x] Task 5: Display Shrine Info on Death Screen (AC: 1, 3) ✅ COMPLETE
  - [x] On the death screen (from Story 2.2), add a line about the shrine: `"{HeroName}'s soul lingers at the death site..."`
  - [x] Show soul energy value: `"Soul Energy: {value}"`
  - [x] This tells the player their sacrifice created something meaningful for future heroes

- [x] Task 6: Show Shrine Details When Adjacent (AC: 3) ✅ COMPLETE
  - [x] When the hero stands on or adjacent to a shrine, show info in the HUD area (row 2 or action log)
  - [x] Format: `"† {HeroName}'s Shrine (Energy: {value}) - Press [b] to bless"` (blessing interaction is Story 2.8)
  - [x] If multiple shrines are adjacent, show the nearest one
  - [x] Implement `getAdjacentShrine(heroX, heroY, shrines)` helper function (similar to `getAdjacentMonster()`)

- [x] Task 7: Create Unit Tests (AC: 1-6) ✅ COMPLETE
  - [x] Create `apps/web/tests/unit/world/SoulShrine.test.ts` (11 tests, all passing)
  - [x] Test `calculateSoulEnergy()` for level 1 hero (minimum 10)
  - [x] Test `calculateSoulEnergy()` scales with hero level and stats
  - [x] Test `createSoulShrine()` generates valid shrine from hero data
  - [x] Test shrine persists to IndexedDB and loads by level depth
  - [x] Test only active shrines are returned by `loadShrinesByLevel()`
  - [x] Test consumed shrine (`isActive = false`) is excluded from level queries
  - [x] Test multiple shrines on same level all load correctly
  - [x] Test shrine creation is integrated into death processing flow
  - [x] Test edge case: hero at level 1 with base stats produces minimum energy shrine

## Dev Notes

### Soul Energy Formula

The soul energy value determines blessing success chance in Story 2.8. A simple scaling formula:

```typescript
function calculateSoulEnergy(hero: Hero): number {
  const baseEnergy = hero.level * 10;
  const statBonus = Math.floor(
    hero.currentStats.strength +
    hero.currentStats.dexterity +
    hero.currentStats.constitution
  );
  return Math.max(10, baseEnergy + statBonus);
}
```

**Example values:**
- Level 1 hero, base stats (10/10/10): `10 + 30 = 40` energy
- Level 3 hero, boosted stats (12/11/13): `30 + 36 = 66` energy
- Level 1 hero, minimum stats: minimum `10` energy

Higher energy = better blessing odds in Story 2.8.

### Shrine ASCII Symbol

The `†` (dagger/cross) symbol is used for shrines. This is a Unicode character (U+2020) that renders well in monospace fonts. If rendering issues arise, fallback to `+` or `$`.

Color: Purple/magenta (`#CC66FF`) to visually distinguish from:
- Monsters (green/yellow/orange/red)
- Teeth (`%` in yellow)
- Chests (`=` in white)
- Hero (`@` in bright green)

### Relationship to Death Processing

The death flow after this story:

```
processHeroDeath(hero, killerMonster, worldState)
  1. Create DeathEvent record
  2. Promote killer monster (Story 2.2)
  3. Transfer equipment to monster (Story 2.3)
  4. Create TeethDrop at death site (Story 2.4)
  5. Create SoulShrine at death site (Story 2.7) ← NEW
  6. Persist all to IndexedDB
  7. Return DeathProcessingResult
```

### Dependencies

- **Story 2.1** — WorldStore/IndexedDB persistence (required for shrine storage)
- **Story 2.2** — DeathProcessor (this story extends it with shrine creation)
- **Story 2.6** — World integration (shrines loaded into levels alongside monsters/teeth)

### Key Files to Modify

| File | Changes |
|------|---------|
| `apps/web/src/world/SoulShrine.ts` | **New** — SoulShrine interface, factory, energy calculation |
| `apps/web/src/world/WorldStore.ts` | Add shrine CRUD methods |
| `apps/web/src/world/DeathProcessor.ts` | Create SoulShrine during death processing |
| `apps/web/src/main.ts` | Load shrines into level, render on map, show adjacent info |
| `apps/web/src/ui/GameHUD.ts` | Add shrine info display (or handle in main.ts render) |
| `apps/web/src/ui/DeathScreen.ts` | Add shrine creation message |

### Testing

- **Test location:** `apps/web/tests/unit/world/`
- **Framework:** Vitest
- **IndexedDB mocking:** Use `fake-indexeddb`
- **Pattern:** Follow existing DeathProcessor and TeethDrop test patterns
- **Key edge cases:** Minimum energy shrine, multiple shrines on same level, consumed shrine excluded from queries

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References
None - Implementation completed without issues

### Completion Notes
- **Soul Energy Calculation:** Formula (level*10) + floor(str+dex+con), minimum 10. Verified with multiple test cases.
- **WorldStore Index Fix:** Fixed shrine index from 'level' to 'levelDepth' to match SoulShrine interface (WorldStore.ts:37)
- **Shrine Rendering:** Shrines render as `†` in purple/magenta (#CC66FF), don't block movement
- **Death Integration:** Shrines created automatically during death processing after teeth drops
- **Adjacent Detection:** `getAdjacentShrine()` finds nearest shrine within 1 tile (including diagonal)
- **Test Coverage:** 11 comprehensive tests covering energy calculation, persistence, filtering, and death integration
- **No breaking changes** - All 302 tests passing (11 new)

### File List
**Created:**
- `apps/web/src/world/SoulShrine.ts` - SoulShrine interface, energy calculation, factory function
- `apps/web/tests/unit/world/SoulShrine.test.ts` - 11 unit tests

**Modified:**
- `apps/web/src/world/WorldStore.ts` - Added loadShrinesByLevel(), consumeShrine(), fixed shrine index
- `apps/web/src/world/DeathProcessor.ts` - Shrine creation during death processing, updated DeathProcessingResult
- `apps/web/src/main.ts` - Shrine loading/rendering, GameState interface, getAdjacentShrine()
- `apps/web/src/ui/DeathScreen.ts` - Shrine creation message on death screen

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-30 | v1.0 | Initial story draft | Sarah (PO Agent) |
| 2026-02-13 | v2.0 | Story complete - All tasks implemented and tested. 302 tests passing (11 new). Status: Ready for Review | James (Dev Agent) |
